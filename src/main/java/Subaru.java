public class Subaru extends Car
{
    public Subaru(int year, String model, String type, boolean isAutomatic, String color, double engineVolume, double accelerationTo100)
    {
        super(year, model, type, isAutomatic, color, engineVolume, accelerationTo100);
    }
}
